{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow the main window to use core IPC and dialog plugin commands for local image editing.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
