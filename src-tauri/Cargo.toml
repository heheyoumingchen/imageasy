[package]
name = "imageasy"
version = "0.1.0"
description = "图片批量处理助手"
authors = ["heheyouchen"]
edition = "2021"
default-run = "imageasy"

[lib]
name = "imageasy_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
anyhow = "1"
base64 = "0.22"
chrono = { version = "0.4", default-features = true, features = ["clock"] }
image = { version = "0.25", default-features = true, features = ["jpeg", "png", "webp", "bmp"] }
jpeg-decoder = "0.3"
lopdf = "0.35"
# 默认 pdfium_latest(=7543) 需要比仓库内 pdfium.dll 更新的导出符号，会导致 GetProcAddress 127。
# 固定到与当前捆绑 pdfium.dll 实测可 bind 的 API 版本。
pdfium-render = { version = "0.8.37", default-features = false, features = ["pdfium_6996", "thread_safe", "image"] }
scraper = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2.0.0", features = ["protocol-asset"] }
tauri-plugin-dialog = "2.0.0"
tauri-plugin-shell = "2.0.0"
tempfile = "3.15"
ureq = "2"
url = "2"
walkdir = "2.5"
rayon = "1.10"
zip = { version = "2", default-features = false, features = ["deflate"] }
webpx = { version = "0.4", default-features = false, features = ["encode", "std"] }
sysinfo = { version = "0.37", default-features = false, features = ["system"] }
tokio = { version = "1", features = ["sync", "rt", "macros", "time"] }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.61", features = [
  "Win32_Foundation",
  "Win32_System_Com",
  "Win32_System_Ole",
  "Win32_System_Variant",
] }

[features]
# 仅用于 conversion_benchmark 示例：编译可复现的性能证据采集代码。
benchmarking = []

[[example]]
name = "conversion_benchmark"
required-features = ["benchmarking"]

[[bin]]
name = "document-renderer-helper"
path = "src/bin/document_renderer_helper.rs"

[dev-dependencies]
