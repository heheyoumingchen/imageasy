import PlaceholderPage from './PlaceholderPage';

const ExtractImagePage = () => {
  return (
    <PlaceholderPage
      title="提取图片"
      description="后续接入 Word 文档抽图与结果统计能力，本轮先保留稳定入口。"
      points={['文档导入区', '输出设置区', '提取结果区']}
    />
  );
};

export default ExtractImagePage;
